services:
  api:
    build: .
    ports:
      - "5001:8080"
    environment:
      - PORT=8080
    volumes:
      - ./src:/app/src
    command: uv run uvicorn uk_budget_data.api:app --host 0.0.0.0 --port 8080 --reload
